''' manipulatin mcfost data'''
from astropy.io import fits

def load_cube(file):
    """ wave, pixsc, dat = getcube(fitsfile)

    Returns the content of an image cube

    PARAMETER:

    file: fits file as generated by mcfost

    OUTPUT:
    wave: wavelength in microns
    pixsc: pixel scale in degrees
    dat: image cube

    EXAMPLE:

    wavelength, pixscale, data = mcfost.utils.getcube('RT.fits.gz')

    """
    datahdu = fits.open(file)
    pixscale = datahdu[0].header['CDELT2']
    wavelength = datahdu[0].header['WAVE'] * 1e-6

    return wavelength, pixscale, datahdu[0].data

